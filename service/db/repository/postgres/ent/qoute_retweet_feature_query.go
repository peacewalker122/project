// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/peacewalker122/project/service/db/repository/postgres/ent/predicate"
	"github.com/peacewalker122/project/service/db/repository/postgres/ent/qoute_retweet_feature"
)

// QouteRetweetFeatureQuery is the builder for querying Qoute_retweet_feature entities.
type QouteRetweetFeatureQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.Qoute_retweet_feature
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the QouteRetweetFeatureQuery builder.
func (qrfq *QouteRetweetFeatureQuery) Where(ps ...predicate.Qoute_retweet_feature) *QouteRetweetFeatureQuery {
	qrfq.predicates = append(qrfq.predicates, ps...)
	return qrfq
}

// Limit adds a limit step to the query.
func (qrfq *QouteRetweetFeatureQuery) Limit(limit int) *QouteRetweetFeatureQuery {
	qrfq.limit = &limit
	return qrfq
}

// Offset adds an offset step to the query.
func (qrfq *QouteRetweetFeatureQuery) Offset(offset int) *QouteRetweetFeatureQuery {
	qrfq.offset = &offset
	return qrfq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (qrfq *QouteRetweetFeatureQuery) Unique(unique bool) *QouteRetweetFeatureQuery {
	qrfq.unique = &unique
	return qrfq
}

// Order adds an order step to the query.
func (qrfq *QouteRetweetFeatureQuery) Order(o ...OrderFunc) *QouteRetweetFeatureQuery {
	qrfq.order = append(qrfq.order, o...)
	return qrfq
}

// First returns the first Qoute_retweet_feature entity from the query.
// Returns a *NotFoundError when no Qoute_retweet_feature was found.
func (qrfq *QouteRetweetFeatureQuery) First(ctx context.Context) (*Qoute_retweet_feature, error) {
	nodes, err := qrfq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{qoute_retweet_feature.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (qrfq *QouteRetweetFeatureQuery) FirstX(ctx context.Context) *Qoute_retweet_feature {
	node, err := qrfq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Qoute_retweet_feature ID from the query.
// Returns a *NotFoundError when no Qoute_retweet_feature ID was found.
func (qrfq *QouteRetweetFeatureQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = qrfq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{qoute_retweet_feature.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (qrfq *QouteRetweetFeatureQuery) FirstIDX(ctx context.Context) int {
	id, err := qrfq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Qoute_retweet_feature entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Qoute_retweet_feature entity is found.
// Returns a *NotFoundError when no Qoute_retweet_feature entities are found.
func (qrfq *QouteRetweetFeatureQuery) Only(ctx context.Context) (*Qoute_retweet_feature, error) {
	nodes, err := qrfq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{qoute_retweet_feature.Label}
	default:
		return nil, &NotSingularError{qoute_retweet_feature.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (qrfq *QouteRetweetFeatureQuery) OnlyX(ctx context.Context) *Qoute_retweet_feature {
	node, err := qrfq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Qoute_retweet_feature ID in the query.
// Returns a *NotSingularError when more than one Qoute_retweet_feature ID is found.
// Returns a *NotFoundError when no entities are found.
func (qrfq *QouteRetweetFeatureQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = qrfq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{qoute_retweet_feature.Label}
	default:
		err = &NotSingularError{qoute_retweet_feature.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (qrfq *QouteRetweetFeatureQuery) OnlyIDX(ctx context.Context) int {
	id, err := qrfq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Qoute_retweet_features.
func (qrfq *QouteRetweetFeatureQuery) All(ctx context.Context) ([]*Qoute_retweet_feature, error) {
	if err := qrfq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return qrfq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (qrfq *QouteRetweetFeatureQuery) AllX(ctx context.Context) []*Qoute_retweet_feature {
	nodes, err := qrfq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Qoute_retweet_feature IDs.
func (qrfq *QouteRetweetFeatureQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := qrfq.Select(qoute_retweet_feature.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (qrfq *QouteRetweetFeatureQuery) IDsX(ctx context.Context) []int {
	ids, err := qrfq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (qrfq *QouteRetweetFeatureQuery) Count(ctx context.Context) (int, error) {
	if err := qrfq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return qrfq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (qrfq *QouteRetweetFeatureQuery) CountX(ctx context.Context) int {
	count, err := qrfq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (qrfq *QouteRetweetFeatureQuery) Exist(ctx context.Context) (bool, error) {
	if err := qrfq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return qrfq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (qrfq *QouteRetweetFeatureQuery) ExistX(ctx context.Context) bool {
	exist, err := qrfq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the QouteRetweetFeatureQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (qrfq *QouteRetweetFeatureQuery) Clone() *QouteRetweetFeatureQuery {
	if qrfq == nil {
		return nil
	}
	return &QouteRetweetFeatureQuery{
		config:     qrfq.config,
		limit:      qrfq.limit,
		offset:     qrfq.offset,
		order:      append([]OrderFunc{}, qrfq.order...),
		predicates: append([]predicate.Qoute_retweet_feature{}, qrfq.predicates...),
		// clone intermediate query.
		sql:    qrfq.sql.Clone(),
		path:   qrfq.path,
		unique: qrfq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		FromAccountID int64 `json:"from_account_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.QouteRetweetFeature.Query().
//		GroupBy(qoute_retweet_feature.FieldFromAccountID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (qrfq *QouteRetweetFeatureQuery) GroupBy(field string, fields ...string) *QouteRetweetFeatureGroupBy {
	grbuild := &QouteRetweetFeatureGroupBy{config: qrfq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := qrfq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return qrfq.sqlQuery(ctx), nil
	}
	grbuild.label = qoute_retweet_feature.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		FromAccountID int64 `json:"from_account_id,omitempty"`
//	}
//
//	client.QouteRetweetFeature.Query().
//		Select(qoute_retweet_feature.FieldFromAccountID).
//		Scan(ctx, &v)
func (qrfq *QouteRetweetFeatureQuery) Select(fields ...string) *QouteRetweetFeatureSelect {
	qrfq.fields = append(qrfq.fields, fields...)
	selbuild := &QouteRetweetFeatureSelect{QouteRetweetFeatureQuery: qrfq}
	selbuild.label = qoute_retweet_feature.Label
	selbuild.flds, selbuild.scan = &qrfq.fields, selbuild.Scan
	return selbuild
}

// Aggregate returns a QouteRetweetFeatureSelect configured with the given aggregations.
func (qrfq *QouteRetweetFeatureQuery) Aggregate(fns ...AggregateFunc) *QouteRetweetFeatureSelect {
	return qrfq.Select().Aggregate(fns...)
}

func (qrfq *QouteRetweetFeatureQuery) prepareQuery(ctx context.Context) error {
	for _, f := range qrfq.fields {
		if !qoute_retweet_feature.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if qrfq.path != nil {
		prev, err := qrfq.path(ctx)
		if err != nil {
			return err
		}
		qrfq.sql = prev
	}
	return nil
}

func (qrfq *QouteRetweetFeatureQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Qoute_retweet_feature, error) {
	var (
		nodes = []*Qoute_retweet_feature{}
		_spec = qrfq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Qoute_retweet_feature).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Qoute_retweet_feature{config: qrfq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, qrfq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (qrfq *QouteRetweetFeatureQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := qrfq.querySpec()
	_spec.Node.Columns = qrfq.fields
	if len(qrfq.fields) > 0 {
		_spec.Unique = qrfq.unique != nil && *qrfq.unique
	}
	return sqlgraph.CountNodes(ctx, qrfq.driver, _spec)
}

func (qrfq *QouteRetweetFeatureQuery) sqlExist(ctx context.Context) (bool, error) {
	switch _, err := qrfq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

func (qrfq *QouteRetweetFeatureQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   qoute_retweet_feature.Table,
			Columns: qoute_retweet_feature.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: qoute_retweet_feature.FieldID,
			},
		},
		From:   qrfq.sql,
		Unique: true,
	}
	if unique := qrfq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := qrfq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, qoute_retweet_feature.FieldID)
		for i := range fields {
			if fields[i] != qoute_retweet_feature.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := qrfq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := qrfq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := qrfq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := qrfq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (qrfq *QouteRetweetFeatureQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(qrfq.driver.Dialect())
	t1 := builder.Table(qoute_retweet_feature.Table)
	columns := qrfq.fields
	if len(columns) == 0 {
		columns = qoute_retweet_feature.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if qrfq.sql != nil {
		selector = qrfq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if qrfq.unique != nil && *qrfq.unique {
		selector.Distinct()
	}
	for _, p := range qrfq.predicates {
		p(selector)
	}
	for _, p := range qrfq.order {
		p(selector)
	}
	if offset := qrfq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := qrfq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// QouteRetweetFeatureGroupBy is the group-by builder for Qoute_retweet_feature entities.
type QouteRetweetFeatureGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (qrfgb *QouteRetweetFeatureGroupBy) Aggregate(fns ...AggregateFunc) *QouteRetweetFeatureGroupBy {
	qrfgb.fns = append(qrfgb.fns, fns...)
	return qrfgb
}

// Scan applies the group-by query and scans the result into the given value.
func (qrfgb *QouteRetweetFeatureGroupBy) Scan(ctx context.Context, v any) error {
	query, err := qrfgb.path(ctx)
	if err != nil {
		return err
	}
	qrfgb.sql = query
	return qrfgb.sqlScan(ctx, v)
}

func (qrfgb *QouteRetweetFeatureGroupBy) sqlScan(ctx context.Context, v any) error {
	for _, f := range qrfgb.fields {
		if !qoute_retweet_feature.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := qrfgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := qrfgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (qrfgb *QouteRetweetFeatureGroupBy) sqlQuery() *sql.Selector {
	selector := qrfgb.sql.Select()
	aggregation := make([]string, 0, len(qrfgb.fns))
	for _, fn := range qrfgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(qrfgb.fields)+len(qrfgb.fns))
		for _, f := range qrfgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(qrfgb.fields...)...)
}

// QouteRetweetFeatureSelect is the builder for selecting fields of QouteRetweetFeature entities.
type QouteRetweetFeatureSelect struct {
	*QouteRetweetFeatureQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (qrfs *QouteRetweetFeatureSelect) Aggregate(fns ...AggregateFunc) *QouteRetweetFeatureSelect {
	qrfs.fns = append(qrfs.fns, fns...)
	return qrfs
}

// Scan applies the selector query and scans the result into the given value.
func (qrfs *QouteRetweetFeatureSelect) Scan(ctx context.Context, v any) error {
	if err := qrfs.prepareQuery(ctx); err != nil {
		return err
	}
	qrfs.sql = qrfs.QouteRetweetFeatureQuery.sqlQuery(ctx)
	return qrfs.sqlScan(ctx, v)
}

func (qrfs *QouteRetweetFeatureSelect) sqlScan(ctx context.Context, v any) error {
	aggregation := make([]string, 0, len(qrfs.fns))
	for _, fn := range qrfs.fns {
		aggregation = append(aggregation, fn(qrfs.sql))
	}
	switch n := len(*qrfs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		qrfs.sql.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		qrfs.sql.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := qrfs.sql.Query()
	if err := qrfs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
