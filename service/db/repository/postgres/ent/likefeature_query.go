// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/peacewalker122/project/service/db/repository/postgres/ent/likefeature"
	"github.com/peacewalker122/project/service/db/repository/postgres/ent/predicate"
)

// LikeFeatureQuery is the builder for querying LikeFeature entities.
type LikeFeatureQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.LikeFeature
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LikeFeatureQuery builder.
func (lfq *LikeFeatureQuery) Where(ps ...predicate.LikeFeature) *LikeFeatureQuery {
	lfq.predicates = append(lfq.predicates, ps...)
	return lfq
}

// Limit adds a limit step to the query.
func (lfq *LikeFeatureQuery) Limit(limit int) *LikeFeatureQuery {
	lfq.limit = &limit
	return lfq
}

// Offset adds an offset step to the query.
func (lfq *LikeFeatureQuery) Offset(offset int) *LikeFeatureQuery {
	lfq.offset = &offset
	return lfq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (lfq *LikeFeatureQuery) Unique(unique bool) *LikeFeatureQuery {
	lfq.unique = &unique
	return lfq
}

// Order adds an order step to the query.
func (lfq *LikeFeatureQuery) Order(o ...OrderFunc) *LikeFeatureQuery {
	lfq.order = append(lfq.order, o...)
	return lfq
}

// First returns the first LikeFeature entity from the query.
// Returns a *NotFoundError when no LikeFeature was found.
func (lfq *LikeFeatureQuery) First(ctx context.Context) (*LikeFeature, error) {
	nodes, err := lfq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{likefeature.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (lfq *LikeFeatureQuery) FirstX(ctx context.Context) *LikeFeature {
	node, err := lfq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LikeFeature ID from the query.
// Returns a *NotFoundError when no LikeFeature ID was found.
func (lfq *LikeFeatureQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = lfq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{likefeature.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (lfq *LikeFeatureQuery) FirstIDX(ctx context.Context) int {
	id, err := lfq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LikeFeature entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LikeFeature entity is found.
// Returns a *NotFoundError when no LikeFeature entities are found.
func (lfq *LikeFeatureQuery) Only(ctx context.Context) (*LikeFeature, error) {
	nodes, err := lfq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{likefeature.Label}
	default:
		return nil, &NotSingularError{likefeature.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (lfq *LikeFeatureQuery) OnlyX(ctx context.Context) *LikeFeature {
	node, err := lfq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LikeFeature ID in the query.
// Returns a *NotSingularError when more than one LikeFeature ID is found.
// Returns a *NotFoundError when no entities are found.
func (lfq *LikeFeatureQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = lfq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{likefeature.Label}
	default:
		err = &NotSingularError{likefeature.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (lfq *LikeFeatureQuery) OnlyIDX(ctx context.Context) int {
	id, err := lfq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LikeFeatures.
func (lfq *LikeFeatureQuery) All(ctx context.Context) ([]*LikeFeature, error) {
	if err := lfq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return lfq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (lfq *LikeFeatureQuery) AllX(ctx context.Context) []*LikeFeature {
	nodes, err := lfq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LikeFeature IDs.
func (lfq *LikeFeatureQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := lfq.Select(likefeature.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (lfq *LikeFeatureQuery) IDsX(ctx context.Context) []int {
	ids, err := lfq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (lfq *LikeFeatureQuery) Count(ctx context.Context) (int, error) {
	if err := lfq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return lfq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (lfq *LikeFeatureQuery) CountX(ctx context.Context) int {
	count, err := lfq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (lfq *LikeFeatureQuery) Exist(ctx context.Context) (bool, error) {
	if err := lfq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return lfq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (lfq *LikeFeatureQuery) ExistX(ctx context.Context) bool {
	exist, err := lfq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LikeFeatureQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (lfq *LikeFeatureQuery) Clone() *LikeFeatureQuery {
	if lfq == nil {
		return nil
	}
	return &LikeFeatureQuery{
		config:     lfq.config,
		limit:      lfq.limit,
		offset:     lfq.offset,
		order:      append([]OrderFunc{}, lfq.order...),
		predicates: append([]predicate.LikeFeature{}, lfq.predicates...),
		// clone intermediate query.
		sql:    lfq.sql.Clone(),
		path:   lfq.path,
		unique: lfq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		FromAccountID int64 `json:"from_account_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.LikeFeature.Query().
//		GroupBy(likefeature.FieldFromAccountID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (lfq *LikeFeatureQuery) GroupBy(field string, fields ...string) *LikeFeatureGroupBy {
	grbuild := &LikeFeatureGroupBy{config: lfq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := lfq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return lfq.sqlQuery(ctx), nil
	}
	grbuild.label = likefeature.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		FromAccountID int64 `json:"from_account_id,omitempty"`
//	}
//
//	client.LikeFeature.Query().
//		Select(likefeature.FieldFromAccountID).
//		Scan(ctx, &v)
func (lfq *LikeFeatureQuery) Select(fields ...string) *LikeFeatureSelect {
	lfq.fields = append(lfq.fields, fields...)
	selbuild := &LikeFeatureSelect{LikeFeatureQuery: lfq}
	selbuild.label = likefeature.Label
	selbuild.flds, selbuild.scan = &lfq.fields, selbuild.Scan
	return selbuild
}

// Aggregate returns a LikeFeatureSelect configured with the given aggregations.
func (lfq *LikeFeatureQuery) Aggregate(fns ...AggregateFunc) *LikeFeatureSelect {
	return lfq.Select().Aggregate(fns...)
}

func (lfq *LikeFeatureQuery) prepareQuery(ctx context.Context) error {
	for _, f := range lfq.fields {
		if !likefeature.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if lfq.path != nil {
		prev, err := lfq.path(ctx)
		if err != nil {
			return err
		}
		lfq.sql = prev
	}
	return nil
}

func (lfq *LikeFeatureQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LikeFeature, error) {
	var (
		nodes = []*LikeFeature{}
		_spec = lfq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LikeFeature).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LikeFeature{config: lfq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, lfq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (lfq *LikeFeatureQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := lfq.querySpec()
	_spec.Node.Columns = lfq.fields
	if len(lfq.fields) > 0 {
		_spec.Unique = lfq.unique != nil && *lfq.unique
	}
	return sqlgraph.CountNodes(ctx, lfq.driver, _spec)
}

func (lfq *LikeFeatureQuery) sqlExist(ctx context.Context) (bool, error) {
	switch _, err := lfq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

func (lfq *LikeFeatureQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   likefeature.Table,
			Columns: likefeature.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: likefeature.FieldID,
			},
		},
		From:   lfq.sql,
		Unique: true,
	}
	if unique := lfq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := lfq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, likefeature.FieldID)
		for i := range fields {
			if fields[i] != likefeature.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := lfq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := lfq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := lfq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := lfq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (lfq *LikeFeatureQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(lfq.driver.Dialect())
	t1 := builder.Table(likefeature.Table)
	columns := lfq.fields
	if len(columns) == 0 {
		columns = likefeature.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if lfq.sql != nil {
		selector = lfq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if lfq.unique != nil && *lfq.unique {
		selector.Distinct()
	}
	for _, p := range lfq.predicates {
		p(selector)
	}
	for _, p := range lfq.order {
		p(selector)
	}
	if offset := lfq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := lfq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// LikeFeatureGroupBy is the group-by builder for LikeFeature entities.
type LikeFeatureGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (lfgb *LikeFeatureGroupBy) Aggregate(fns ...AggregateFunc) *LikeFeatureGroupBy {
	lfgb.fns = append(lfgb.fns, fns...)
	return lfgb
}

// Scan applies the group-by query and scans the result into the given value.
func (lfgb *LikeFeatureGroupBy) Scan(ctx context.Context, v any) error {
	query, err := lfgb.path(ctx)
	if err != nil {
		return err
	}
	lfgb.sql = query
	return lfgb.sqlScan(ctx, v)
}

func (lfgb *LikeFeatureGroupBy) sqlScan(ctx context.Context, v any) error {
	for _, f := range lfgb.fields {
		if !likefeature.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := lfgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lfgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (lfgb *LikeFeatureGroupBy) sqlQuery() *sql.Selector {
	selector := lfgb.sql.Select()
	aggregation := make([]string, 0, len(lfgb.fns))
	for _, fn := range lfgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(lfgb.fields)+len(lfgb.fns))
		for _, f := range lfgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(lfgb.fields...)...)
}

// LikeFeatureSelect is the builder for selecting fields of LikeFeature entities.
type LikeFeatureSelect struct {
	*LikeFeatureQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (lfs *LikeFeatureSelect) Aggregate(fns ...AggregateFunc) *LikeFeatureSelect {
	lfs.fns = append(lfs.fns, fns...)
	return lfs
}

// Scan applies the selector query and scans the result into the given value.
func (lfs *LikeFeatureSelect) Scan(ctx context.Context, v any) error {
	if err := lfs.prepareQuery(ctx); err != nil {
		return err
	}
	lfs.sql = lfs.LikeFeatureQuery.sqlQuery(ctx)
	return lfs.sqlScan(ctx, v)
}

func (lfs *LikeFeatureSelect) sqlScan(ctx context.Context, v any) error {
	aggregation := make([]string, 0, len(lfs.fns))
	for _, fn := range lfs.fns {
		aggregation = append(aggregation, fn(lfs.sql))
	}
	switch n := len(*lfs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		lfs.sql.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		lfs.sql.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := lfs.sql.Query()
	if err := lfs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
