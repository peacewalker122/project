// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"github.com/peacewalker122/project/service/db/repository/postgres/ent/notifread"
	"github.com/peacewalker122/project/service/db/repository/postgres/ent/predicate"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// NotifReadQuery is the builder for querying NotifRead entities.
type NotifReadQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.NotifRead
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the NotifReadQuery builder.
func (nrq *NotifReadQuery) Where(ps ...predicate.NotifRead) *NotifReadQuery {
	nrq.predicates = append(nrq.predicates, ps...)
	return nrq
}

// Limit adds a limit step to the query.
func (nrq *NotifReadQuery) Limit(limit int) *NotifReadQuery {
	nrq.limit = &limit
	return nrq
}

// Offset adds an offset step to the query.
func (nrq *NotifReadQuery) Offset(offset int) *NotifReadQuery {
	nrq.offset = &offset
	return nrq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (nrq *NotifReadQuery) Unique(unique bool) *NotifReadQuery {
	nrq.unique = &unique
	return nrq
}

// Order adds an order step to the query.
func (nrq *NotifReadQuery) Order(o ...OrderFunc) *NotifReadQuery {
	nrq.order = append(nrq.order, o...)
	return nrq
}

// First returns the first NotifRead entity from the query.
// Returns a *NotFoundError when no NotifRead was found.
func (nrq *NotifReadQuery) First(ctx context.Context) (*NotifRead, error) {
	nodes, err := nrq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{notifread.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (nrq *NotifReadQuery) FirstX(ctx context.Context) *NotifRead {
	node, err := nrq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first NotifRead ID from the query.
// Returns a *NotFoundError when no NotifRead ID was found.
func (nrq *NotifReadQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = nrq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{notifread.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (nrq *NotifReadQuery) FirstIDX(ctx context.Context) int {
	id, err := nrq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single NotifRead entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one NotifRead entity is found.
// Returns a *NotFoundError when no NotifRead entities are found.
func (nrq *NotifReadQuery) Only(ctx context.Context) (*NotifRead, error) {
	nodes, err := nrq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{notifread.Label}
	default:
		return nil, &NotSingularError{notifread.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (nrq *NotifReadQuery) OnlyX(ctx context.Context) *NotifRead {
	node, err := nrq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only NotifRead ID in the query.
// Returns a *NotSingularError when more than one NotifRead ID is found.
// Returns a *NotFoundError when no entities are found.
func (nrq *NotifReadQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = nrq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{notifread.Label}
	default:
		err = &NotSingularError{notifread.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (nrq *NotifReadQuery) OnlyIDX(ctx context.Context) int {
	id, err := nrq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of NotifReads.
func (nrq *NotifReadQuery) All(ctx context.Context) ([]*NotifRead, error) {
	if err := nrq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return nrq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (nrq *NotifReadQuery) AllX(ctx context.Context) []*NotifRead {
	nodes, err := nrq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of NotifRead IDs.
func (nrq *NotifReadQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := nrq.Select(notifread.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (nrq *NotifReadQuery) IDsX(ctx context.Context) []int {
	ids, err := nrq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (nrq *NotifReadQuery) Count(ctx context.Context) (int, error) {
	if err := nrq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return nrq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (nrq *NotifReadQuery) CountX(ctx context.Context) int {
	count, err := nrq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (nrq *NotifReadQuery) Exist(ctx context.Context) (bool, error) {
	if err := nrq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return nrq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (nrq *NotifReadQuery) ExistX(ctx context.Context) bool {
	exist, err := nrq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the NotifReadQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (nrq *NotifReadQuery) Clone() *NotifReadQuery {
	if nrq == nil {
		return nil
	}
	return &NotifReadQuery{
		config:     nrq.config,
		limit:      nrq.limit,
		offset:     nrq.offset,
		order:      append([]OrderFunc{}, nrq.order...),
		predicates: append([]predicate.NotifRead{}, nrq.predicates...),
		// clone intermediate query.
		sql:    nrq.sql.Clone(),
		path:   nrq.path,
		unique: nrq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		NotifID uuid.UUID `json:"notif_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.NotifRead.Query().
//		GroupBy(notifread.FieldNotifID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (nrq *NotifReadQuery) GroupBy(field string, fields ...string) *NotifReadGroupBy {
	grbuild := &NotifReadGroupBy{config: nrq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := nrq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return nrq.sqlQuery(ctx), nil
	}
	grbuild.label = notifread.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		NotifID uuid.UUID `json:"notif_id,omitempty"`
//	}
//
//	client.NotifRead.Query().
//		Select(notifread.FieldNotifID).
//		Scan(ctx, &v)
func (nrq *NotifReadQuery) Select(fields ...string) *NotifReadSelect {
	nrq.fields = append(nrq.fields, fields...)
	selbuild := &NotifReadSelect{NotifReadQuery: nrq}
	selbuild.label = notifread.Label
	selbuild.flds, selbuild.scan = &nrq.fields, selbuild.Scan
	return selbuild
}

// Aggregate returns a NotifReadSelect configured with the given aggregations.
func (nrq *NotifReadQuery) Aggregate(fns ...AggregateFunc) *NotifReadSelect {
	return nrq.Select().Aggregate(fns...)
}

func (nrq *NotifReadQuery) prepareQuery(ctx context.Context) error {
	for _, f := range nrq.fields {
		if !notifread.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if nrq.path != nil {
		prev, err := nrq.path(ctx)
		if err != nil {
			return err
		}
		nrq.sql = prev
	}
	return nil
}

func (nrq *NotifReadQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*NotifRead, error) {
	var (
		nodes = []*NotifRead{}
		_spec = nrq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*NotifRead).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &NotifRead{config: nrq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, nrq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (nrq *NotifReadQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := nrq.querySpec()
	_spec.Node.Columns = nrq.fields
	if len(nrq.fields) > 0 {
		_spec.Unique = nrq.unique != nil && *nrq.unique
	}
	return sqlgraph.CountNodes(ctx, nrq.driver, _spec)
}

func (nrq *NotifReadQuery) sqlExist(ctx context.Context) (bool, error) {
	switch _, err := nrq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

func (nrq *NotifReadQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   notifread.Table,
			Columns: notifread.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: notifread.FieldID,
			},
		},
		From:   nrq.sql,
		Unique: true,
	}
	if unique := nrq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := nrq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, notifread.FieldID)
		for i := range fields {
			if fields[i] != notifread.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := nrq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := nrq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := nrq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := nrq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (nrq *NotifReadQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(nrq.driver.Dialect())
	t1 := builder.Table(notifread.Table)
	columns := nrq.fields
	if len(columns) == 0 {
		columns = notifread.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if nrq.sql != nil {
		selector = nrq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if nrq.unique != nil && *nrq.unique {
		selector.Distinct()
	}
	for _, p := range nrq.predicates {
		p(selector)
	}
	for _, p := range nrq.order {
		p(selector)
	}
	if offset := nrq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := nrq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// NotifReadGroupBy is the group-by builder for NotifRead entities.
type NotifReadGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (nrgb *NotifReadGroupBy) Aggregate(fns ...AggregateFunc) *NotifReadGroupBy {
	nrgb.fns = append(nrgb.fns, fns...)
	return nrgb
}

// Scan applies the group-by query and scans the result into the given value.
func (nrgb *NotifReadGroupBy) Scan(ctx context.Context, v any) error {
	query, err := nrgb.path(ctx)
	if err != nil {
		return err
	}
	nrgb.sql = query
	return nrgb.sqlScan(ctx, v)
}

func (nrgb *NotifReadGroupBy) sqlScan(ctx context.Context, v any) error {
	for _, f := range nrgb.fields {
		if !notifread.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := nrgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := nrgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (nrgb *NotifReadGroupBy) sqlQuery() *sql.Selector {
	selector := nrgb.sql.Select()
	aggregation := make([]string, 0, len(nrgb.fns))
	for _, fn := range nrgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(nrgb.fields)+len(nrgb.fns))
		for _, f := range nrgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(nrgb.fields...)...)
}

// NotifReadSelect is the builder for selecting fields of NotifRead entities.
type NotifReadSelect struct {
	*NotifReadQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (nrs *NotifReadSelect) Aggregate(fns ...AggregateFunc) *NotifReadSelect {
	nrs.fns = append(nrs.fns, fns...)
	return nrs
}

// Scan applies the selector query and scans the result into the given value.
func (nrs *NotifReadSelect) Scan(ctx context.Context, v any) error {
	if err := nrs.prepareQuery(ctx); err != nil {
		return err
	}
	nrs.sql = nrs.NotifReadQuery.sqlQuery(ctx)
	return nrs.sqlScan(ctx, v)
}

func (nrs *NotifReadSelect) sqlScan(ctx context.Context, v any) error {
	aggregation := make([]string, 0, len(nrs.fns))
	for _, fn := range nrs.fns {
		aggregation = append(aggregation, fn(nrs.sql))
	}
	switch n := len(*nrs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		nrs.sql.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		nrs.sql.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := nrs.sql.Query()
	if err := nrs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
